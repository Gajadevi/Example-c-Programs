#include <stdio.h>

struct patient {
    int pid;
    char name[20];
    int age;
};

int main() {
    struct patient p;

    printf("Enter patient id: ");
    scanf("%d", &p.pid);

    printf("Enter patient name: ");
    scanf("%s", p.name);

    printf("Enter age: ");
    scanf("%d", &p.age);

    printf("\nPatient Details:\n");
    printf("ID: %d\nName: %s\nAge: %d",
           p.pid, p.name, p.age);

    return 0;
}
