#include <stdio.h>

int main() {
    int i;
    float sales[7], total = 0;

    for(i = 0; i < 7; i++) {
        scanf("%f", &sales[i]);
        total += sales[i];
    }

    printf("Total Sales = %.2f", total);
    return 0;
}
