#include <stdio.h>

float toFahrenheit(float c) {
    return (c * 9 / 5) + 32;
}

int main() {
    float c;

    printf("Enter temperature in Celsius: ");
    scanf("%f", &c);

    printf("Fahrenheit = %.2f", toFahrenheit(c));

    return 0;
}
